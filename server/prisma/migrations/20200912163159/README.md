# Migration `20200912163159`

This migration has been generated by Alaa Zorkane at 9/12/2020, 5:31:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Message" (
"id" text   NOT NULL ,
"content" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
"channelId" text   ,
"userId" text   NOT NULL ,
PRIMARY KEY ("id")
)

ALTER TABLE "public"."Message" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Message" ADD FOREIGN KEY ("channelId")REFERENCES "public"."Channel"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200912161101..20200912163159
--- datamodel.dml
+++ datamodel.dml
@@ -1,26 +1,39 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
+  id        String    @id @default(cuid())
+  username  String    @unique
+  password  String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  Channel   Channel?  @relation(fields: [channelId], references: [id])
+  channelId String?
+  Message   Message[]
+}
+
+model Message {
   id        String   @id @default(cuid())
-  username  String   @unique
-  password  String
+  content   String
+  user      User     @relation(fields: [userId], references: [id])
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   Channel   Channel? @relation(fields: [channelId], references: [id])
   channelId String?
+  userId    String
 }
 model Channel {
-  id        String   @id @default(cuid())
+  id        String    @id @default(cuid())
   name      String
   members   User[]
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+  messages  Message[]
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
 }
```


